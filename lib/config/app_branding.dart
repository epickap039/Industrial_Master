/// Debe coincidir con la línea `version:` de [pubspec.yaml]:
/// `MAJOR.MINOR.PATCH+BUILD` — el número tras `+` es el build mostrado como IMv.
const int kAppBuildNumber = 327;

/// Título de ventana (`FluentApp`) y leyenda junto al ícono en `NavigationAppBar`.
String get kAppChromeTitle => 'INGENIERIA IMv$kAppBuildNumber';
